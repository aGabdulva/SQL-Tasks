Перечислите названия головных кораблей, имеющихся в базе данных (учесть корабли в Outcomes).

SELECT name
FROM Ships
WHERE name = class

UNION

SELECT ship
FROM Outcomes
WHERE ship IN (SELECT class FROM classes)
OR ship IN (SELECT class FROM Ships)
