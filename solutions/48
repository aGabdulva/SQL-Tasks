Найдите классы кораблей, в которых хотя бы один корабль был потоплен в сражении.

SELECT DISTINCT c.class AS clas
FROM Classes c
JOIN Ships s
ON c.class = s.class
JOIN Outcomes o
ON o.ship = s.name
WHERE o.result = 'sunk'

UNION

SELECT DISTINCT c.class AS clas
FROM Classes c
JOIN Outcomes o
ON o.ship = c.class
WHERE o.result = 'sunk'
